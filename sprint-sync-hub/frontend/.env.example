# Backend API URL
VITE_API_URL=http://localhost:3001

# Must match backend's API_AUTH_TOKEN. Leave blank only if the backend also
# has API_AUTH_TOKEN unset (local dev with an unprotected API).
VITE_API_KEY=
